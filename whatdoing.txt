current app structure:
PatternSeer
| PatternRenderer <implements CallbackTrait>
| | paint, prepare
| hello_triangle

this structure means that hello_triangle is only accesible to the PatternRenderer on creation.
if hello_triangle was ever updated later, the data would be inaccesible


Wait no hold on, if we provide the PatternRenderer with an update function that can pass in the
new hello_triangle that can be called each frame in the PatternSeer.ui() function


hmmm no actually we don't want to do that. we don't want to store any data inside PatternRenderer.
Plus, then we would have two copies of data that we would have to keep in sync

What about this app structure:

PatternSeer
| PatternRenderer
| | new(wgpu_render_state)
| | render_hello_triangle(vertices, frame) -> PatternRendererCallback
| vertices

then each frame in ui() we call PatternRenderer.render_hello_triangle() to get a new RendererCallback. 


then each frame we need to
    - get all the grid lines in view
        min_x = ceil(Camera.X - (Camera.Width / 2*Camera.Zoom))
        max_x = floor(Camera.X - (Camera.Width / 2*Camera.Zoom))
        min_y = ceil(Camera.Y - (Camera.Height / 2*Camera.Zoom))
        max_y = floor(Camera.Y - (Camera.Height / 2*Camera.Zoom))
    - convert their world-space coordinates to clip space
    - generate a quad for each line in clip space
    - send these quads into the render pipeline to be rendered


also we need to create the render pipeline with the shaders



