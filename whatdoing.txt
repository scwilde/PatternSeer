--- Bug Fixes ---
-[] Slight aliasing when very slowly panning the grid while zoomed out to right before a grid level change
-[] Sometimes the cutout corners come back


--- Easy stuff for if allergic to progress ---
-[] Add confirmation dialogue when closing while not saved
-[] Move the "save as if never saved before" functionality into the pattern_file::save() function
-[] remove passing the pattern to menubar and instead just pass a boolean for if the pattern is present
-[] Decouple Pattern from Camera's `limit_pan()`, `limit_zoom()`, and `fit_to_pattern()` functions
-[] Remove the gridline struct and have GridlineIter just emit Quads
-[] Add back a draft trait to FormTSM to improve documentation
-[] Document functions with side effects
-[] `NothingHappened` is an irrelevant PatternFormEvent variant
-[X] Create CommandQueue struct that wraps a HashMap that hashes an enum variant discriminant to an instance of that variant

--- House Keeping ---
-[] Update Documentation


--- Current Work: Pattern loading & Rendering ---
-[] create a test pattern with some cells filled
-[] Set pattern max size to 4096 x 4096
-[] Upon loading pattern, pull the entire grid into vram
    -[] Create a new render pipeline with a buffer big enough to hold the new pattern
    -[] Upload the pattern to that buffer
    -[] Add a save button that pulls everything from the buffer and dumps it back into the sqlite file
-[] Create pattern renderer

Mipmap level = floor(log_2(1/zoom) + 1), clamped to  1 - 6

--- Basic brush system and pattern editing ---

-[] Add ability to edit pattern one pixel at a time
-[] Add increased brush size
-[] Add delta interpolation of brush

--- Basic Floss & color system ---
!! Can we release 0.1 here??? !!
--- Special Stitches; Backstitching, /4 stitches, petite, fr knots ---
--- Custom symbol system ---
--- Meat and potatoes: Digitization tools ---
--- Meat and potatoes: Color interpolation tools ---
--- Meat and potatoes: Pixel art tools like dithering ---


--- Scope/API creep ---
- Signaling API
    - What if we want to instruct Editor to do things and send it along some data as well?
    - What if we want to instruct a retained mode UI element that has already run its course for this frame to do something on the next frame?
    - What if we want to instruct an immediate mode UI element that has already run its course for this frame to do something on the next frame?
