--- Bug Fixes ---
- [] Slight aliasing when very slowly panning the grid while zoomed out to right before a grid level change

--- Current Work ---

-[] Housekeeping
    -[X] Remove leftovers from old API
    -[X] Update documentation
    -[X] Clean up variable and field names
    -[] Utility for handling bounds
-[] geometry API
    -[X] Colors
    -[] positions
    -[X] verts
    -[] quads
    -[] tris
-[] Move the zoom level calculation into the camera and then call it each time the camera zooms only

-- Pattern loading -- 

-[] Add ability to load a pattern from an SQLite DB
    -[] Pattern should be divided into 64x64 tiles
    -[] We need a cache of tiles currently in view
        - What do we do when the entie canvas is in view? We don't want to load every single tile, do we?
          - Nope, we do not; My guestimate is that that would be about 1.5 Gb of ram with a max canvas size of 10k x 10k

-- Pattern rendering (easy ?)

-[] Add ability to render that pattern
    -[] Basic zoomed in rendering
    -[] Mipmap rendering when zoomed out

-- Basic brush system and pattern editing --

-[] Add ability to edit pattern one pixel at a time
-[] Add increased brush size
-[] Add delta interpolation of brush

-- Basic Floss & color system --
!! Can we release 0.1 here??? !!
-- Special Stitches; Backstitching, /4 stitches, petite --
-- Meat and potatoes: Digitization tools --
-- Meat and potatoes: Color interpolation tools --
-- Meat and potatoes: Pixel art tools like dithering --
