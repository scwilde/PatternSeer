-[X] change triangle to gridlines
    - [X] when camera moves, mark it as dirty
    - [X] Send canvas to Renderer
    - [X] only do all this if the camera is dirty
        - [X] Gridline culling. Renderer generates gridline limits within view as an iterator.
            these are world-space gridline positions
                min_x = ceil(Camera.X - (Camera.Width / 2*Camera.Zoom)) | clamped to 0
                max_x = floor(Camera.X - (Camera.Width / 2*Camera.Zoom)) | clamped to canvas.width
                min_y = ceil(Camera.Y - (Camera.Height / 2*Camera.Zoom)) | clamped to 0
                max_y = floor(Camera.Y - (Camera.Height / 2*Camera.Zoom)) | clamped to canvas.height
        - [X] next we iterate over each grid line
            - [X] get gridline's x or y position in space
            - [X] generate an `offset` value. 1x grids have an offset of 1px, 10x have an offset of 2px,
                gridlines 0 and canvas width/height have an offset of 3.
            - [X] for x gridlines produce the vertices
                (precalculate x-offset/2 and x+offset/2)
                [
                    [x-offset/2, -1],
                    [x+offset/2, -1],
                    [x-offset/2, 1],
                    [x+offset/2, -1],
                    [x+offset/2, 1],
                    [x-offset/2, 1],
                ]
            - [X] and for y produce these vertices
                (precalculate y-offset/2 and y+offset/2)
                [
                    [-1, y+offset/2],
                    [1, y+offset/2],
                    [1, y-offset/2],
                    [-1, y+offset/2],
                    [1, y-offset/2],
                    [-1, y-offset/2],
                ]
            - [X] Collapse all these vertices into a vec and save into `callback_resources`; mark vertices dirty
        - [X] In the prepare stage, if the vertices are dirty upload them to the gpu
- [X] Set grid lines to be bound by pattern edges
- [X] Change grid to be black on white
- [X] Stop the user from wandering too far from the canvas
- [X] Stop the user from zooming too far in or out
- [X] change line's width if it is a multiple of 10
- [] cull any squares that are smaller than 4 px
- [] shift the line width levels down when this culling happens


then each frame we need to
    - get all the grid lines in view
        
    - convert their world-space coordinates to clip space
    - generate a quad for each line in clip space
    - send these quads into the render pipeline to be rendered


also we need to create the render pipeline with the shaders



