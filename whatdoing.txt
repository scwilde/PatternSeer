
-[] Housekeeping
    -[~] Change all the arrays into VecNs
        Mostly done; unsure how to do camera.position_bounds
    -[] Update documentation
    -[X] Pop renderer for the grid out into its own thing
-[] geometry API
    -[] Colors
    -[] positions
    -[] bounds
    -[] quads
    -[] tris
-[] Move the zoom level calculation into the camera and then call it each time the camera zooms only

-- GPU API --

-[] Abstract all renderer code into its own API
    - The goal is that if I change what backend GPU API I use, I only have to change the implementation of this
      API, rather than changing anything about the actual renderers.
    - Also, try to avoid scope creep please; only write stuff we have an immediate need for; ***IMMEDIATE*** need
    -[] We create a GPU context *once* and store it inside the App
    -[] Renderers are sent the GPU context
    -[] Renderers attach their own <Renderer>Resources class to the callback_resources of the GPU context
    -[] Some sort of GPU utils class will provide abstraction over creating pipelines and managing other Resources
    -[] Each renderer will have a Mesh class or something similar that manages the buffer
         lifetime and the dirtiness of its data

-- Pattern loading -- 

-[] Add ability to load a pattern from an SQLite DB
    -[] Pattern should be divided into 64x64 tiles
    -[] We need a cache of tiles currently in view
        - What do we do when the entie canvas is in view? We don't want to load every single tile, do we?
          - Nope, we do not; My guestimate is that that would be about 1.5 Gb of ram with a max canvas size of 10k x 10k

-- Pattern rendering (easy ?)

-[] Add ability to render that pattern
    -[] Basic zoomed in rendering
    -[] Mipmap rendering when zoomed out

-- Basic brush system and pattern editing --

-[] Add ability to edit pattern one pixel at a time
-[] Add increased brush size
-[] Add delta interpolation of brush

-- Floss & color system --
!! Can we release 1.0 here??? !!
-- Special Stitches; Backstitching, /4 stitches, petite --
-- Meat and potatoes: Digitization tools --
-- Meat and potatoes: Pixel art tools like dithering --
