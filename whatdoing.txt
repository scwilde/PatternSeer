--- Bug Fixes ---
-[] Slight aliasing when very slowly panning the grid while zoomed out to right before a grid level change
-[] Sometimes the cutout corners come back


--- Easy stuff for if allergic to progress ---
-[] Add confirmation dialogue when closing while not saved
-[] Move the "save as if never saved before" functionality into the pattern_file::save() function
-[] remove passing the pattern to menubar and instead just pass a boolean for if the pattern is present
-[] Decouple Pattern from Camera's `limit_pan()`, `limit_zoom()`, and `fit_to_pattern()` functions
-[] Remove the gridline struct and have GridlineIter just emit Quads
-[] Add back a Draft trait to FormTSM to improve documentation
-[] Document functions' side effects
-[] `NothingHappened` is an irrelevant PatternFormEvent variant
-[X] Create CommandBuffer struct
-[] Add a pattern properties panel on the side to allow resizing of pattern, renaming, changing designer, etc.


--- Current Work: Pattern loading & Rendering ---
-[] create a test pattern with some cells filled
-[] Set pattern max size to 4096 x 4096; Double the value when its converted to the real thing
-[X] data structure changes
    -[X] PrimaryGrid -> StitchBuffer. This makes it much more clear its a linear collection of stitches
-[] File I/O changes
    -[X] Only load the chunk data if the chunk type is one that needs loading (i.e. a known chunk or an unknown but protected chunk)
    -[X] Ditch the chunk iterator; Just loop over the file until we reach the end or we get a truncated chunk
    -[X] Handle EOF properly
        -[X] UnexpectedEOF while reading a fourcc means a clean end of file and None should be returned; a partial fourcc should just be silently discarded
                A deliberate design descision needs to be made on whether the presence of a partial fourcc is cause for a malformed file, and if it is not cause for a malformed file then a descision needs to be made on whether the entire wrapper, both fourcc and chunk length, should be silently discarded. It is almost definite though that a complete chunk wrapper but an incomplete chunk body means malformed data.
        -[X] UnexpectedEOF while reading the chunk length signifies malformed data if it is a known chunk
        -[X] UnexpectedEOF while reading the chunk body signifies malformed data
    -[] Write most of the read_chunk() tests
    -[] write a few load() tests?
    -[] Add validation logic that the created StitchBuffer is actually a size that matches the header size
    -[] finish tests?
    
-[] Fix the UI
-[] Start cleaning stuff up
    -[] Create clean APIs
        -[] pattern
            -[] Move the actual pattern into its own submodule
            -[] Expose the io submodule as well as any of the types
        -[] Decouple things that don't really need to be coupled
            -[] decouple pattern from the editor camera
            -[] decouple file selection logic from the UI code; it all lives inside App
-[] Data structure changes
    -[] Stitch palette
        -[] Make stitch types into an enum with various data attached to them
    -[X] Color palette
    -[X] Stitch buffer thats a 2d array of indexes into StitchPalette
-[] Fix warnings
-[] Update documentation
-[] Pattern loading
    -[] First load pattern into memory
    -[] Create a new render pipeline with a buffer big enough to hold the new pattern
    -[] Upload the pattern to that buffer
-[] Create pattern renderer

Mipmap level = floor(log_2(1/zoom) + 1), clamped to  1 - 6

--- Basic brush system and pattern editing ---

-[] Add ability to edit pattern one pixel at a time
-[] Add increased brush size
-[] Add delta interpolation of brush

--- Basic Floss & color system ---
!! Can we release 0.1 here??? !!
--- Special Stitches; Backstitching, /4 stitches, petite, fr knots ---
--- Custom symbol system ---
--- Meat and potatoes: Digitization tools ---
--- Meat and potatoes: Color interpolation tools ---
--- Meat and potatoes: Pixel art tools like dithering ---


--- Scope/API creep ---
- Signaling API
    - What if we want to instruct Editor to do things and send it along some data as well?
    - What if we want to instruct a retained mode UI element that has already run its course for this frame to do something on the next frame?
    - What if we want to instruct an immediate mode UI element that has already run its course for this frame to do something on the next frame?
